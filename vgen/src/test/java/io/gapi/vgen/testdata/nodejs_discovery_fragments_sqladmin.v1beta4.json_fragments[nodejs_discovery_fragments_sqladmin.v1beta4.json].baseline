
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'delete' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";
    // * The ID of the Backup Run to delete. To find a Backup Run ID, use the list method.
    request.id = '';

    sqladmin.backupRuns.delete(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'get' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";
    // * The ID of this Backup Run.
    request.id = '';

    sqladmin.backupRuns.get(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'list' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";


    var recur = function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
        if (result.nextPageToken) {
          request.pageToken = result.nextPageToken;
          sqladmin.backupRuns.list(request, recur);
        }
      }
    };

    sqladmin.backupRuns.list(request, recur);
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'delete' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Database instance ID. This does not include the project ID.
    request.instance = "";
    // * Name of the database to be deleted in the instance.
    request.database = "";

    sqladmin.databases.delete(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'get' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Database instance ID. This does not include the project ID.
    request.instance = "";
    // * Name of the database in the instance.
    request.database = "";

    sqladmin.databases.get(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'insert' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Database instance ID. This does not include the project ID.
    request.instance = "";
    request.resource = {};

    sqladmin.databases.insert(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'list' method:

    // * Project ID of the project for which to list Cloud SQL instances.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";

    sqladmin.databases.list(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'patch' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Database instance ID. This does not include the project ID.
    request.instance = "";
    // * Name of the database to be updated in the instance.
    request.database = "";
    request.resource = {};

    sqladmin.databases.patch(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'update' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Database instance ID. This does not include the project ID.
    request.instance = "";
    // * Name of the database to be updated in the instance.
    request.database = "";
    request.resource = {};

    sqladmin.databases.update(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'list' method:


    sqladmin.flags.list(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'clone' method:

    // * Project ID of the source as well as the clone Cloud SQL instance.
    request.project = "";
    // * The ID of the Cloud SQL instance to be cloned (source). This does not include the project ID.
    request.instance = "";
    request.resource = {};

    sqladmin.instances.clone(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'delete' method:

    // * Project ID of the project that contains the instance to be deleted.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";

    sqladmin.instances.delete(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'export' method:

    // * Project ID of the project that contains the instance to be exported.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";
    request.resource = {};

    sqladmin.instances.export(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'failover' method:

    // * ID of the project that contains the read replica.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";
    request.resource = {};

    sqladmin.instances.failover(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'get' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Database instance ID. This does not include the project ID.
    request.instance = "";

    sqladmin.instances.get(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'import' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";
    request.resource = {};

    sqladmin.instances.import(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'insert' method:

    // * Project ID of the project to which the newly created Cloud SQL instances should belong.
    request.project = "";
    request.resource = {};

    sqladmin.instances.insert(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'list' method:

    // * Project ID of the project for which to list Cloud SQL instances.
    request.project = "";


    var recur = function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
        if (result.nextPageToken) {
          request.pageToken = result.nextPageToken;
          sqladmin.instances.list(request, recur);
        }
      }
    };

    sqladmin.instances.list(request, recur);
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'patch' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";
    request.resource = {};

    sqladmin.instances.patch(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'promoteReplica' method:

    // * ID of the project that contains the read replica.
    request.project = "";
    // * Cloud SQL read replica instance name.
    request.instance = "";

    sqladmin.instances.promoteReplica(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'resetSslConfig' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";

    sqladmin.instances.resetSslConfig(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'restart' method:

    // * Project ID of the project that contains the instance to be restarted.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";

    sqladmin.instances.restart(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'restoreBackup' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";
    request.resource = {};

    sqladmin.instances.restoreBackup(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'startReplica' method:

    // * ID of the project that contains the read replica.
    request.project = "";
    // * Cloud SQL read replica instance name.
    request.instance = "";

    sqladmin.instances.startReplica(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'stopReplica' method:

    // * ID of the project that contains the read replica.
    request.project = "";
    // * Cloud SQL read replica instance name.
    request.instance = "";

    sqladmin.instances.stopReplica(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'update' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";
    request.resource = {};

    sqladmin.instances.update(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'get' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Instance operation ID.
    request.operation = "";

    sqladmin.operations.get(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'list' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";


    var recur = function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
        if (result.nextPageToken) {
          request.pageToken = result.nextPageToken;
          sqladmin.operations.list(request, recur);
        }
      }
    };

    sqladmin.operations.list(request, recur);
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'createEphemeral' method:

    // * Project ID of the Cloud SQL project.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";
    request.resource = {};

    sqladmin.sslCerts.createEphemeral(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'delete' method:

    // * Project ID of the project that contains the instance to be deleted.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";
    // * Sha1 FingerPrint.
    request.sha1Fingerprint = "";

    sqladmin.sslCerts.delete(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'get' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";
    // * Sha1 FingerPrint.
    request.sha1Fingerprint = "";

    sqladmin.sslCerts.get(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'insert' method:

    // * Project ID of the project to which the newly created Cloud SQL instances should belong.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";
    request.resource = {};

    sqladmin.sslCerts.insert(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'list' method:

    // * Project ID of the project for which to list Cloud SQL instances.
    request.project = "";
    // * Cloud SQL instance ID. This does not include the project ID.
    request.instance = "";

    sqladmin.sslCerts.list(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'list' method:

    // * Project ID of the project for which to list tiers.
    request.project = "";

    sqladmin.tiers.list(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'delete' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Database instance ID. This does not include the project ID.
    request.instance = "";
    // * Host of the user in the instance.
    request.host = "";
    // * Name of the user in the instance.
    request.name = "";

    sqladmin.users.delete(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'insert' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Database instance ID. This does not include the project ID.
    request.instance = "";
    request.resource = {};

    sqladmin.users.insert(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'list' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Database instance ID. This does not include the project ID.
    request.instance = "";

    sqladmin.users.list(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
var google = require('googleapis');
var sqladmin = google.sqladmin('v1beta4');
var GoogleAuth = require('google-auth-library');

(new GoogleAuth).getApplicationDefault(function(err, authClient) {
  if (err === null) {
    if (authClient.createScopedRequired && authClient.createScopedRequired()) {
      var scopes = ['https://www.googleapis.com/auth/cloud-platform'];
      authClient = authClient.createScoped(scopes);
    }

    var request = {
      auth: authClient
    };

    // TODO: Change placeholders below to values for parameters to the 'update' method:

    // * Project ID of the project that contains the instance.
    request.project = "";
    // * Database instance ID. This does not include the project ID.
    request.instance = "";
    // * Host of the user in the instance.
    request.host = "";
    // * Name of the user in the instance.
    request.name = "";
    request.resource = {};

    sqladmin.users.update(request, function(error, result) {
      if (error) {
        console.log(error);
      } else {
        console.log(result);
      }
    });
   }
});
