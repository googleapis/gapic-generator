# TODO(jgeiger): move to non-Python-specific location.
# Provides common utility functions.

#
# A logical and.
#
@snippet and(a, b) horizontal
  @if a
    @if b
      and: true
    @end
  @end
@end

# Logic "not"
@snippet not(a) horizontal
  @if a
  @else
    not: true
  @end
@end