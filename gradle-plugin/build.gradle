apply plugin: 'groovy'
apply plugin: 'java-gradle-plugin'

repositories {
  maven {
    url 'https://oss.sonatype.org/content/repositories/snapshots/'
  }
}

dependencies {
  compile project(':vgen'),
    libraries.protobufGradlePlugin
}
