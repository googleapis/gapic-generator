
<?php
/*
 * PRE-REQUISITES:
 * ---------------
 * 1. If not already done, enable the Google Dataflow API and check quota for your project at
 *    https://console.developers.google.com/apis/api/dataflow_component/quotas
 * 2. To install the client library on Composer, check installation instructions at
 *    https://github.com/google/google-api-php-client.
 * 3. This sample uses Application Default Credentials for Auth. If not already done, install the gcloud CLI from
 *    https://cloud.google.com/sdk/ and run 'gcloud beta auth application-default login'
 */

// composer autoloading
require_once __DIR__ . '/vendor/autoload.php';


function DataflowExample
{
  // Create a new client
  $client = new Google_Client();
  $client->setApplicationName('Client Sample Application');
  $client->useApplicationDefaultCredentials();
  $client->addScope('https://www.googleapis.com/auth/cloud-platform');

  // Create a new Dataflow service
  $service = new Google_Service_Dataflow($client);

  // The project which owns the job.
  $projectId = '';

  $response = $service->projects->jobs->createJobs($projectId);
}

<?php
/*
 * PRE-REQUISITES:
 * ---------------
 * 1. If not already done, enable the Google Dataflow API and check quota for your project at
 *    https://console.developers.google.com/apis/api/dataflow_component/quotas
 * 2. To install the client library on Composer, check installation instructions at
 *    https://github.com/google/google-api-php-client.
 * 3. This sample uses Application Default Credentials for Auth. If not already done, install the gcloud CLI from
 *    https://cloud.google.com/sdk/ and run 'gcloud beta auth application-default login'
 */

// composer autoloading
require_once __DIR__ . '/vendor/autoload.php';


function DataflowExample
{
  // Create a new client
  $client = new Google_Client();
  $client->setApplicationName('Client Sample Application');
  $client->useApplicationDefaultCredentials();
  $client->addScope('https://www.googleapis.com/auth/cloud-platform');

  // Create a new Dataflow service
  $service = new Google_Service_Dataflow($client);

  // The project which owns the job.
  $projectId = '';
  // Identifies a single job.
  $jobId = '';

  $response = $service->projects->jobs->getJobs($projectId, $jobId);
}

<?php
/*
 * PRE-REQUISITES:
 * ---------------
 * 1. If not already done, enable the Google Dataflow API and check quota for your project at
 *    https://console.developers.google.com/apis/api/dataflow_component/quotas
 * 2. To install the client library on Composer, check installation instructions at
 *    https://github.com/google/google-api-php-client.
 * 3. This sample uses Application Default Credentials for Auth. If not already done, install the gcloud CLI from
 *    https://cloud.google.com/sdk/ and run 'gcloud beta auth application-default login'
 */

// composer autoloading
require_once __DIR__ . '/vendor/autoload.php';


function DataflowExample
{
  // Create a new client
  $client = new Google_Client();
  $client->setApplicationName('Client Sample Application');
  $client->useApplicationDefaultCredentials();
  $client->addScope('https://www.googleapis.com/auth/cloud-platform');

  // Create a new Dataflow service
  $service = new Google_Service_Dataflow($client);

  // A project id.
  $projectId = '';
  // The job to get messages for.
  $jobId = '';

  $response = $service->projects->jobs->getMetricsJobs($projectId, $jobId);
}

<?php
/*
 * PRE-REQUISITES:
 * ---------------
 * 1. If not already done, enable the Google Dataflow API and check quota for your project at
 *    https://console.developers.google.com/apis/api/dataflow_component/quotas
 * 2. To install the client library on Composer, check installation instructions at
 *    https://github.com/google/google-api-php-client.
 * 3. This sample uses Application Default Credentials for Auth. If not already done, install the gcloud CLI from
 *    https://cloud.google.com/sdk/ and run 'gcloud beta auth application-default login'
 */

// composer autoloading
require_once __DIR__ . '/vendor/autoload.php';


function DataflowExample
{
  // Create a new client
  $client = new Google_Client();
  $client->setApplicationName('Client Sample Application');
  $client->useApplicationDefaultCredentials();
  $client->addScope('https://www.googleapis.com/auth/cloud-platform');

  // Create a new Dataflow service
  $service = new Google_Service_Dataflow($client);

  // The project which owns the jobs.
  $projectId = '';

  $response = $service->projects->jobs->listJobs($projectId);

  foreach ($response['jobs'] as $jobs) {
    // TODO: Add code here to process jobs.
  }
}

<?php
/*
 * PRE-REQUISITES:
 * ---------------
 * 1. If not already done, enable the Google Dataflow API and check quota for your project at
 *    https://console.developers.google.com/apis/api/dataflow_component/quotas
 * 2. To install the client library on Composer, check installation instructions at
 *    https://github.com/google/google-api-php-client.
 * 3. This sample uses Application Default Credentials for Auth. If not already done, install the gcloud CLI from
 *    https://cloud.google.com/sdk/ and run 'gcloud beta auth application-default login'
 */

// composer autoloading
require_once __DIR__ . '/vendor/autoload.php';


function DataflowExample
{
  // Create a new client
  $client = new Google_Client();
  $client->setApplicationName('Client Sample Application');
  $client->useApplicationDefaultCredentials();
  $client->addScope('https://www.googleapis.com/auth/cloud-platform');

  // Create a new Dataflow service
  $service = new Google_Service_Dataflow($client);

  // A project id.
  $projectId = '';
  // The job to get messages about.
  $jobId = '';

  $response = $service->projects->jobs->messages->listMessages($projectId, $jobId);

  foreach ($response['jobMessages'] as $jobMessages) {
    // TODO: Add code here to process jobMessages.
  }
}

<?php
/*
 * PRE-REQUISITES:
 * ---------------
 * 1. If not already done, enable the Google Dataflow API and check quota for your project at
 *    https://console.developers.google.com/apis/api/dataflow_component/quotas
 * 2. To install the client library on Composer, check installation instructions at
 *    https://github.com/google/google-api-php-client.
 * 3. This sample uses Application Default Credentials for Auth. If not already done, install the gcloud CLI from
 *    https://cloud.google.com/sdk/ and run 'gcloud beta auth application-default login'
 */

// composer autoloading
require_once __DIR__ . '/vendor/autoload.php';


function DataflowExample
{
  // Create a new client
  $client = new Google_Client();
  $client->setApplicationName('Client Sample Application');
  $client->useApplicationDefaultCredentials();
  $client->addScope('https://www.googleapis.com/auth/cloud-platform');

  // Create a new Dataflow service
  $service = new Google_Service_Dataflow($client);

  // The project which owns the job.
  $projectId = '';
  // Identifies a single job.
  $jobId = '';

  $response = $service->projects->jobs->updateJobs($projectId, $jobId);
}

<?php
/*
 * PRE-REQUISITES:
 * ---------------
 * 1. If not already done, enable the Google Dataflow API and check quota for your project at
 *    https://console.developers.google.com/apis/api/dataflow_component/quotas
 * 2. To install the client library on Composer, check installation instructions at
 *    https://github.com/google/google-api-php-client.
 * 3. This sample uses Application Default Credentials for Auth. If not already done, install the gcloud CLI from
 *    https://cloud.google.com/sdk/ and run 'gcloud beta auth application-default login'
 */

// composer autoloading
require_once __DIR__ . '/vendor/autoload.php';


function DataflowExample
{
  // Create a new client
  $client = new Google_Client();
  $client->setApplicationName('Client Sample Application');
  $client->useApplicationDefaultCredentials();
  $client->addScope('https://www.googleapis.com/auth/cloud-platform');

  // Create a new Dataflow service
  $service = new Google_Service_Dataflow($client);

  // Identifies the project this worker belongs to.
  $projectId = '';
  // Identifies the workflow job this worker belongs to.
  $jobId = '';

  $response = $service->projects->jobs->workItems->leaseWorkItems($projectId, $jobId);
}

<?php
/*
 * PRE-REQUISITES:
 * ---------------
 * 1. If not already done, enable the Google Dataflow API and check quota for your project at
 *    https://console.developers.google.com/apis/api/dataflow_component/quotas
 * 2. To install the client library on Composer, check installation instructions at
 *    https://github.com/google/google-api-php-client.
 * 3. This sample uses Application Default Credentials for Auth. If not already done, install the gcloud CLI from
 *    https://cloud.google.com/sdk/ and run 'gcloud beta auth application-default login'
 */

// composer autoloading
require_once __DIR__ . '/vendor/autoload.php';


function DataflowExample
{
  // Create a new client
  $client = new Google_Client();
  $client->setApplicationName('Client Sample Application');
  $client->useApplicationDefaultCredentials();
  $client->addScope('https://www.googleapis.com/auth/cloud-platform');

  // Create a new Dataflow service
  $service = new Google_Service_Dataflow($client);

  // The project which owns the WorkItem's job.
  $projectId = '';
  // The job which the WorkItem is part of.
  $jobId = '';

  $response = $service->projects->jobs->workItems->reportStatusWorkItems($projectId, $jobId);
}

<?php
/*
 * PRE-REQUISITES:
 * ---------------
 * 1. If not already done, enable the Google Dataflow API and check quota for your project at
 *    https://console.developers.google.com/apis/api/dataflow_component/quotas
 * 2. To install the client library on Composer, check installation instructions at
 *    https://github.com/google/google-api-php-client.
 * 3. This sample uses Application Default Credentials for Auth. If not already done, install the gcloud CLI from
 *    https://cloud.google.com/sdk/ and run 'gcloud beta auth application-default login'
 */

// composer autoloading
require_once __DIR__ . '/vendor/autoload.php';


function DataflowExample
{
  // Create a new client
  $client = new Google_Client();
  $client->setApplicationName('Client Sample Application');
  $client->useApplicationDefaultCredentials();
  $client->addScope('https://www.googleapis.com/auth/cloud-platform');

  // Create a new Dataflow service
  $service = new Google_Service_Dataflow($client);

  // The project to send the WorkerMessages to.
  $projectId = '';

  $response = $service->projects->workerMessagesProjects($projectId);
}

