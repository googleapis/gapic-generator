@snippet generateFilename()
    README.rst
@end

@snippet generateBody(model)
    gRPC library for google-{@context.getShortName}-{@context.getVersion}

    {@context.getPackageName} is the IDL-derived library for the {@context.getShortName} ({@context.getVersion}) service in the googleapis_ repository.

    .. _`googleapis`: https://github.com/googleapis/googleapis/tree/master/{@context.getPath}/{@context.getVersion}

@end
