@snippet generate(metadata)
    gRPC library for {@metadata.fullName}

    {@metadata.protoPackageName} is the IDL-derived library for the {@metadata.shortName} ({@metadata.majorVersion}) service in the googleapis_ repository.

    .. _`googleapis`: https://github.com/googleapis/googleapis/tree/master/{@metadata.protoPath}

@end
