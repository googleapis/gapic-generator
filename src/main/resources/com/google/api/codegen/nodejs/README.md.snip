@extends "common.snip"

@snippet generate(metadata)
  {@generateReadme(metadata.readmeMetadata)}
@end

@private generateReadme(metadata)
  @# {@metadata.targetLanguage} \
      Client{@simplePlurality(metadata.hasMultipleServices)} for {@metadata.fullName}
  This is a generated README.md placeholder. Put your own documentation here.
@end
