@snippet generate(protoList)
  [
    @join stub : protoList.stubs on {@","}.add(BREAK)
      "{@protoPath(protoList)}/{@stub.protoFileName}"
    @end
  ]
@end

@private protoPath(protoList)
  @if protoList.fileHeader.hasVersion
    ../../protos
  @else
    ../protos
  @end
@end
