@extends "csharp/common.snip"

@snippet generate(sampleEntryPoint)
    {@initialComments(sampleEntryPoint.fileHeader)}

    using Google.Cloud.SampleUtil;

    namespace {@sampleEntryPoint.fileHeader.packageName}.Samples
    {
        class Program
        {
            static void Main(string[] args) => ApplicationChooser.Run(args);
        }
    }

@end