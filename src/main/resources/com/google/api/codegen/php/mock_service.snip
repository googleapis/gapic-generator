@extends "php/common.snip"

@snippet generate(mockService)
    {@renderFileHeader(mockService.fileHeader, simpleGeneratedCodeWarning())}

    class {@mockService.serviceImpl.name} extends {@mockService.serviceImpl.grpcClassName}
    {
        use MockStubTrait;
    }
@end
