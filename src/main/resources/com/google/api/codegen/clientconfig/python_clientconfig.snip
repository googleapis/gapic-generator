@extends "common.snip"
@extends "clientconfig/common.snip"

@snippet generateFilename(service)
    {@context.upperCamelToLowerUnderscore(service.getSimpleName)}_client_config.py
@end

@snippet generateBody(service)
    config = {
      {@clientConfig(service)}
    }

@end
