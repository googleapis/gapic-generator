sudo: false
language: java
env:
  global:
    - GRADLE_OPTS="-Xmx512m"
jdk:
  - oraclejdk8
script:
  - ./gradlew check --info
  - ./testutils/check-baselines.sh
  - go run testutils/check-calling-form-in-baseline.go -yaml src/test/java/com/google/api/codegen/testsrc/library_gapic.yaml src
after_success:
  - bash <(curl -s https://codecov.io/bash)
